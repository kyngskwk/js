// 비구조화. 해체한다!
const name = student.name
const 

actions: {
  increment ({ commit }) {
    commit('increment')
  },
  checkout ({ commit, state }, products) {
    // 장바구니에 있는 항목을 저장
    const scaedCardItems = [ ...state.cart.added] 


  }
}
